# userscript-building

Test repository with 2 methods of building userscripts

* `yarn webpack` puts builds into dist `bundle.meta.js` and `bundle.user.js`
* `yarn rollup` puts build into dist `bundle.js`
